{
  "name": "cellxgene",
  "scripts": {
    "postinstall": "npm install --prefix client && npm run build --prefix client -- ../server/eb/customize/webpack.config.js && make copy-client-assets && rm -rf client/node_modules && make -C server/eb build"
  }
}
